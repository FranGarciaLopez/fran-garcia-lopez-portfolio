<header
          id="landing-header"
          class="fixed top-0 right-0 z-50 py-1 px-3 w-full flex justify-end items-center"
>
          <!-- Menu Button -->
          <button
                    id="menu-button"
                    class="fixed top-6 right-6 flex items-center px-5 py-3 rounded-full bg-black/30 text-white hover:bg-black/50 transition-colors duration-300"
          >
                    <svg class="w-8 h-8" fill="none" viewBox="0 0 24 24">
                              <path
                                        stroke="currentColor"
                                        stroke-linecap="round"
                                        stroke-linejoin="round"
                                        stroke-width="2"
                                        d="M4 6h16M4 12h16m-7 6h7"></path>
                    </svg>
          </button>

          <!-- Primary Menu -->
          <nav
                    id="primary-menu"
                    class="fixed top-0 left-0 w-full h-full bg-black/70 backdrop-blur-xl z-50 flex flex-col items-center justify-center text-black transform -translate-y-full transition-transform duration-500 ease-in-out"
          >
                    <!-- Close Button -->
                    <button
                              id="close-menu"
                              class="absolute top-6 right-6 flex items-center px-5 py-3 rounded-full bg-black/30 text-white hover:bg-black/50 transition-colors duration-300"
                    >
                              <svg
                                        class="w-8 h-8"
                                        fill="white"
                                        viewBox="0 0 24 24"
                              >
                                        <path
                                                  stroke="currentColor"
                                                  stroke-linecap="round"
                                                  stroke-linejoin="round"
                                                  stroke-width="2"
                                                  d="M6 18L18 6M6 6l12 12"
                                        ></path>
                              </svg>
                    </button>

                    <!-- Menu Items -->
                    <ul
                              class="flex flex-col items-center space-y-12 text-black"
                    >
                              <li>
                                        <a
                                                  href="#projects"
                                                  class="text-7xl text-[#eae0d5] font-serif tracking-wide hover:opacity-80"
                                                  >Works</a
                                        >
                              </li>
                              <li>
                                        <a
                                                  href="#about"
                                                  class="text-7xl text-[#eae0d5] font-serif tracking-wide hover:opacity-80"
                                                  >About</a
                                        >
                              </li>
                              <li>
                                        <a
                                                  href="#contact"
                                                  class="text-7xl text-[#eae0d5] font-serif tracking-wide hover:opacity-80"
                                                  >Contact</a
                                        >
                              </li>
                    </ul>

                    <!-- Social Icons -->
                    <div class="absolute bottom-10 flex space-x-6 text-black">
                              <a
                                        href="#"
                                        class="text-2xl hover:opacity-80 transition-all duration-300"
                                        >📷</a
                              >
                              <a
                                        href="#"
                                        class="text-2xl hover:opacity-80 transition-all duration-300"
                                        >🎨</a
                              >
                              <a
                                        href="#"
                                        class="text-2xl hover:opacity-80 transition-all duration-300"
                                        >💼</a
                              >
                              <a
                                        href="#"
                                        class="text-2xl hover:opacity-80 transition-all duration-300"
                                        >📄</a
                              >
                    </div>
          </nav>
</header>

<script>
          document.addEventListener("astro:page-load", () => {
                    const menuButton = document.getElementById("menu-button");
                    const closeMenuButton =
                              document.getElementById("close-menu");
                    const primaryMenu = document.getElementById("primary-menu");

                    if (menuButton && closeMenuButton && primaryMenu) {
                              menuButton.addEventListener("click", function () {
                                        primaryMenu.classList.remove(
                                                  "-translate-y-full",
                                        );
                                        primaryMenu.classList.add(
                                                  "translate-y-0",
                                        );
                                        menuButton.classList.add("hidden");
                              });

                              closeMenuButton.addEventListener(
                                        "click",
                                        function () {
                                                  primaryMenu.classList.remove(
                                                            "translate-y-0",
                                                  );
                                                  primaryMenu.classList.add(
                                                            "-translate-y-full",
                                                  );
                                                  menuButton.classList.remove(
                                                            "hidden",
                                                  );
                                        },
                              );
                    }
          });
</script>
