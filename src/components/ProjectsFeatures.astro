---
import Layout from "../layouts/Layout.astro";
interface AlgorithmFeature {
          description: string;
          benefits?: string[];
          steps?: string[];
}

interface AlgorithmFeatures {
          [key: string]: AlgorithmFeature;
}

const { algorithm_features } = Astro.props as {
          algorithm_features: AlgorithmFeatures;
};
---

<div class="space-y-8">
          <h2
                    class="text-[5vh] md:text-[5vw] font-serif leading-tight tracking-wide text-[#f5f5dc] uppercase text-start"
          >
                    Algorithm <span class="italic text-[#eae0d5]">Features</span
                    >
          </h2>
          {
                    Object.entries(algorithm_features).map(([key, feature]) => (
                              <div
                                        class="border border-[#44484d] p-8 rounded-lg bg-[#1a1c1e] 
                                        transform transition-all duration-500 
                                        hover:bg-gradient-to-t from-[#2c2f36] via-[#3c4049] to-[#4a4e58]
                                        hover:shadow-2xl hover:scale-105"
                              >
                                        <h3 class="text-3xl font-serif tracking-wide text-[#f5f5dc] capitalize">
                                                  {key.replace(/_/g, " ")}
                                        </h3>
                                        <p class="text-lg text-[#a1a1aa] mt-4 leading-relaxed">
                                                  {feature.description}
                                        </p>

                                        {feature.benefits && (
                                                  <ul class="list-disc list-inside text-[#c2b280] mt-4">
                                                            {feature.benefits.map(
                                                                      (
                                                                                benefit,
                                                                      ) => (
                                                                                <li class="leading-relaxed">
                                                                                          {
                                                                                                    benefit
                                                                                          }
                                                                                </li>
                                                                      ),
                                                            )}
                                                  </ul>
                                        )}

                                        {feature.steps && (
                                                  <ol class="list-decimal list-inside text-[#eae0d5] mt-4">
                                                            {feature.steps.map(
                                                                      (
                                                                                step,
                                                                      ) => (
                                                                                <li class="leading-relaxed">
                                                                                          {
                                                                                                    step
                                                                                          }
                                                                                </li>
                                                                      ),
                                                            )}
                                                  </ol>
                                        )}
                              </div>
                    ))
          }
</div>
